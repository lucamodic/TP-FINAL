CREATE SCHEMA IF NOT EXISTS respuestados;
USE respuestados;
CREATE TABLE user(
    name VARCHAR(255),
    spawn VARCHAR(255),
    sex VARCHAR(255),
    mail VARCHAR(255),
    password VARCHAR(255),
    username VARCHAR(255),
    image VARCHAR(255)
);